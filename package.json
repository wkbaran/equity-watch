{
  "name": "equity-watch",
  "version": "1.0.0",
  "description": "Watch equities for price, moving-average, and volume events, and queue what needs a decision.",
  "type": "module",
  "bin": {
    "equity-watch": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run",
    "cli": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.1131.0",
    "@aws-sdk/client-s3": "^3.1131.0",
    "commander": "^12.1.0",
    "csv-parse": "^5.5.6",
    "csv-stringify": "^6.5.1",
    "dotenv": "^17.4.2",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
